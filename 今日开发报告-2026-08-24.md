# Agentpad 今日开发报告｜2026-08-24

## 项目概述

今天完成了将 CXT Studio 12E4 客制化小键盘升级为 **Agentpad** 的首个可用版本。它是一块面向多 Agent 协作的实体控制台：可用按键切换 Agent、用灯效直观看到任务状态，并通过本机飞书、Codex 与 VS Code Claude 工作流使用。

## 今日完成

- 完成并验证 12E4 Agentpad 固件刷写，保留原厂固件以便随时恢复。
- 建立 8 个 Agent 的实体按键布局：探春、黛玉、湘云、香菱、莺儿、Codex、VS Code Claude、宝钗。
- 建立状态灯语言：白色呼吸表示待机、蓝色表示工作、绿色表示完成、琥珀色呼吸表示等待处理、红色闪烁表示异常。
- 完成本机常驻客户端：支持 USB/HID 通讯、开机自启、键盘断线重连与状态查询。
- 接入飞书状态群的实时监听。远程 Agent 只需向群内发送标准状态消息，本机键盘即可同步显示；无需将本机与 Mac Mini 建立额外网络通道。
- 完成多 Agent 状态链路验证，已验证远程 Agent 的真实开始与完成状态能够驱动对应按键。
- 完成 Codex 与 Claude 的本机状态适配基础：任务开始、结束、需要输入和异常可反馈到键盘。
- 完成底部功能键与 4 个旋钮：模型切换、思考深度、系统音量、屏幕缩放，以及语音、批准、拒绝、新任务入口。
- 增加状态自动回收机制：旧任务状态在长时间没有新事件后会自动回到待机，避免按键长期遗留在不同颜色。

## 使用方式

键盘连接在用户当前使用的 Mac 上。飞书状态由本机飞书监听服务接收，远程 Agent 无论运行在哪台机器，只要能向指定飞书群发送状态消息，就能同步其工作状态。因此用户在任何可上网地点使用这台 Mac，都能看到并切换 Agent。

## 当前边界

- 飞书状态为实时推送，新启动监听服务不会回放历史消息。
- “批准”“拒绝”“新任务”目前安全地定位到本机对应飞书会话；若需一键自动发送具体内容，需为各 Agent 约定统一、可审计的命令协议。
- Codex 桌面端没有公开的全局任务状态流；当前状态适配覆盖 Agentpad 自己管理的 Codex 会话。Claude 的状态适配依赖其本机 Hook 生命周期。

## 下一阶段

下一阶段可在不改动硬件的前提下，统一各 Agent 的任务命令协议，并按实际使用习惯细调灯效、快捷键和旋钮策略。
