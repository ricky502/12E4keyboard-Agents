# Agentpad 浏览器倍速适配器

Chrome 加载 `agentpad-playback-adapter` 后，四号旋钮进入播放速度模式时：

- `]`：加速 0.25×
- `[`：减速 0.25×
- 范围：0.25×–3×

适用 YouTube 和 B 站网页视频。普通页面或未进入播放模式时，四号旋钮仍然调文字大小。

安装方式：Chrome 打开 `chrome://extensions`，开启“开发者模式”，选择“加载已解压的扩展程序”，选中本目录下的 `agentpad-playback-adapter` 文件夹。
