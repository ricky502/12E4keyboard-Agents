# AI 硬件训练营作业：AgentPad 多 Agent 与媒体控制键盘

> 提交人：老杨  
> 硬件：CXT Studio 12E4 客制化键盘  
> 主控：ATmega32U4  
> 项目地址：[12E4keyboard-Agents](https://github.com/ricky502/12E4keyboard-Agents)  
> 当前版本：v12.1

## 一、作业要求与完成方式

本次作业选择“参考课程案例，跑通完整开发流程，并在复刻基础上完成自己的功能改造”。课程要求最终提交一段效果展示视频，并说明制作过程和思考。

我参考了案例一“这个键盘哄我每天上工”的核心思路：键盘不只是输入工具，还可以通过实体按键、灯光和反馈帮助用户进入工作状态。在此基础上，我没有简单复制案例，而是结合自己的日常工作，把一块 CXT Studio 12E4 小键盘改造成 **AgentPad 多 Agent 工作台**，并进一步加入视频、音频和网页内容的实体控制功能。

本项目已经完成固件编译、真机刷写、本机客户端安装、飞书状态接入、浏览器扩展适配和实际操作验证。

## 二、为什么要做 AgentPad

我的日常工作中会同时使用多个 AI Agent：有些通过飞书机器人运行，有些是本机 Codex，还有 VS Code 中的 Claude Code。过去主要有三个问题：

1. Agent 分散在不同窗口里，切换时需要反复寻找；
2. 长任务运行时，很难一眼判断谁正在工作、谁已经完成、谁在等待输入；
3. 看 YouTube、B 站课程或处理音视频内容时，音量、暂停、倍速和文字大小需要频繁使用鼠标。

因此，我希望让一个实体设备同时解决“多 Agent 调度”和“媒体内容控制”两个问题：上面的按键负责 Agent，下面的功能键负责高频命令，右侧旋钮负责连续调节。

## 三、最终实现效果

### 1. 八个 Agent 的实体入口

键盘上方两排共八个按键，布局如下：

| 位置 | 1 | 2 | 3 | 4 |
| --- | --- | --- | --- | --- |
| 第一排 | 探春 | 黛玉 | 湘云 | 香菱 |
| 第二排 | 莺儿 | Codex | Claude Code | 宝钗 |

按下飞书 Agent 键，会打开本机飞书中对应的机器人会话；按下 Codex 或 Claude Code 键，会切换到本机对应的工作入口。键盘只控制我当前使用的 Mac，不需要和运行远程 Agent 的 Mac Mini 建立局域网、Tailscale 或其他直连通道。因此，只要本机能够正常使用飞书和互联网，在不同地点也能继续使用。

### 2. 用灯光显示工作状态

我为八个 Agent 键设计了一套统一的状态语言：

| 灯光 | 状态含义 |
| --- | --- |
| 白色呼吸 | Agent 空闲、可用 |
| 蓝色常亮 | 正在思考、执行任务或调用工具 |
| 绿色常亮 | 任务已完成，等待查看 |
| 琥珀色呼吸 | 需要用户输入、批准或选择 |
| 红色闪烁 | 任务异常或执行错误 |

飞书 Agent 在状态群内发送标准生命周期消息，本机监听器收到后将状态转换为灯光。例如：

```text
[AGENTPAD] {"agent":"tanchun","state":"thinking","task_id":"agentpad-test"}
```

任务完成后，对应按键变成绿色；按一下这个绿色按键表示“结果已查看”，灯光恢复为白色。这样，灯光不仅是提示，还形成了“工作—完成—确认”的完整交互闭环。

本机 Codex 和 VS Code Claude Code 则通过本地状态监测与生命周期 Hook 接入同一套状态系统。无论 Agent 在飞书还是本地工具中运行，使用者看到的颜色语义保持一致。

### 3. 底部四个高频功能键

| 功能键 | 实际动作 |
| --- | --- |
| 语音 | Option 键，可配合本机语音输入功能使用 |
| 同意 | Enter 键，用于确认和批准 |
| 停止 | 自动输入 `/stop` 并回车 |
| 新任务 | 自动输入 `/new` 并回车 |

这里特别处理了中文输入法环境：`/stop` 和 `/new` 使用 Unicode 文本注入，避免斜杠被中文输入法打成顿号。

### 4. 四个旋钮

四个旋钮按从左到右排列，当前功能如下：

| 旋钮 | 旋转功能 | 按压功能 |
| --- | --- | --- |
| 1 号 | 在 Codex 中切换模型 | 开启/关闭小键盘灯光 |
| 2 号 | 调整 Codex 思考深度 | 让 Mac 进入睡眠 |
| 3 号 | 调整系统音量 | 播放/暂停 |
| 4 号 | 默认调整文字大小；速度模式下调整视频倍速 | 切换“文字大小/播放速度”模式 |

这种布局把两类工作放在同一块面板上：前两个旋钮服务 AI 工作流，后两个旋钮服务音视频与内容阅读。

## 四、媒体播放功能的重点改造

### 1. 为什么需要播放速度旋钮

我平时主要在浏览器中观看 YouTube 和 B 站。学习类视频经常需要在 1×、1.25×、1.5×、2×之间切换。使用鼠标打开倍速菜单会打断观看，普通键盘快捷键又不容易记忆，因此我把 4 号旋钮做成了双模式控制器：

- 文字模式：旋转时放大或缩小网页文字；
- 播放模式：旋转时以 0.25× 为一档调整视频速度；
- 按下旋钮：在两种模式之间切换，并在 macOS 上显示模式通知；
- 倍速范围：0.25×–3×。

3 号旋钮则负责最常见的音频操作：旋转调节系统音量，按压播放或暂停。这样看视频时，一只手就能完成音量、暂停和倍速控制。

### 2. 浏览器适配

不同网站对播放快捷键的支持并不一致。为此，项目增加了一个本地 Chrome 扩展，只在 YouTube 和 B 站页面生效。扩展寻找当前页面中的 HTML5 `video` 元素，接收 4 号旋钮发送的专用事件，修改 `playbackRate`，并在页面上短暂显示当前倍速。

这个方案的优点是：

- 不依赖网站倍速菜单的位置；
- YouTube 和 B 站使用统一的旋钮操作；
- 不读取、不上传视频内容；
- 非目标网页不会执行倍速操作。

### 3. 解决“倍速变化时页面也缩放”的问题

早期版本中，固件会直接发送系统缩放快捷键，同时本机客户端也会处理旋钮事件。结果是在播放速度模式下，视频速度变化的同时，网页也会放大或缩小。

最终的 v12 固件把四个旋钮统一改为 **Raw HID 事件上报**：固件只告诉客户端“哪个旋钮向哪个方向转了一格”，不再自行发送媒体或缩放快捷键。客户端根据当前模式决定执行文字缩放还是视频倍速，从根源上消除了两个动作同时发生的问题。

### 4. 解决旋钮反应慢的问题

功能正确后，我又发现旋钮每转一格要等待数秒。排查发现，客户端为了判断当前是不是 YouTube 或 B 站，会在每个刻度上向 Chrome 查询一次当前网址；在本机实测，这一步约需 4.7 秒。

优化后的方案不再逐格查询网址，而是直接通过 macOS Quartz 把扩展专用按键发送给前台浏览器。扩展本身已经限制了适用网站，因此无需在客户端重复判断。这样既保留了安全边界，也让连续旋转接近即时响应。

## 五、技术实现与完整开发链路

### 1. 硬件与固件层

- 硬件：CXT Studio 12E4；
- MCU：ATmega32U4；
- 输入结构：4×4 矩阵，其中 12 个带灯按键，另有 4 个旋钮按压位；
- 灯光：12 颗 WS2812 RGB LED；
- 固件框架：QMK；
- 通信：USB HID + Raw HID；
- 当前固件：`cxt_studio_12e4_agentpad_v12_rawhid_encoder.hex`。

固件负责扫描按键和旋钮、接收灯光指令、发送 32 字节 Raw HID 数据包。按键事件、旋钮方向和灯光控制均使用统一协议。

### 2. 本机客户端层

macOS 常驻客户端负责：

- 自动识别键盘并在拔出、重新插入后恢复连接；
- 接收飞书 Agent 的实时状态事件；
- 采集本地 Codex 与 Claude Code 的生命周期状态；
- 把状态映射为每个按键的颜色和灯效；
- 将按键、旋钮操作转换成本机应用动作；
- 提供本地健康检查和键位配置页面；
- 开机自动启动，异常退出后由 macOS 自动拉起。

### 3. 为什么采用“固件 + 客户端 + 浏览器扩展”三层结构

- 固件层追求稳定，只负责硬件输入输出；
- 客户端层适合处理飞书、Codex、系统音量和模式切换；
- 浏览器扩展最了解网页中的视频元素，适合处理倍速。

把功能放在合适的层级后，后续调整 Agent、颜色、快捷键和播放逻辑时，大多数情况不需要重新刷机。只有改变底层 HID 行为时才需要更新固件。

## 六、制作过程与思考

这次开发不是一次成功，而是经过多轮真机测试逐步收敛：

1. **先理解原项目和备份**：核对原键位、矩阵、旋钮物理顺序和刷机方式；
2. **建立最小可用链路**：先做到按键可识别、灯光可控制、键盘可断线重连；
3. **接入 Agent 状态**：把飞书和本地 Agent 的不同状态统一为同一套颜色；
4. **逐个校准旋钮**：真实记录四个旋钮的硬件编号，解决物理顺序与软件编号不一致的问题；
5. **增加媒体模式**：从文字缩放扩展到 YouTube、B 站倍速控制；
6. **处理双重动作**：修改固件，让旋钮只发送 Raw HID；
7. **优化体验**：测出 Chrome 查询耗时，改为直接事件通道，将功能从“能用”提升到“愿意每天用”。

我在这个过程中最大的体会是：AI 硬件的价值不是把所有功能都塞进设备，而是让反馈变得明确、稳定、可预期。一个灯如果经常显示错误状态，比没有灯更容易误导；一个旋钮如果功能很多但反应迟钝，也不会形成肌肉记忆。因此，开发后半程的重点不是继续增加按钮，而是校准状态、消除重复动作、降低延迟。

## 七、作业要求对照

| 课程要求 | 本项目完成情况 |
| --- | --- |
| 参考课程案例完成复刻 | 复刻了“实体按键 + 灯光反馈 + 工作仪式感”的核心交互 |
| 在案例上完成自己的改造 | 改造成八 Agent 调度台，并加入媒体播放双模式旋钮 |
| 跑通完整开发流程 | 已完成需求分析、固件修改、编译、刷写、客户端开发、浏览器适配和真机验证 |
| 展示最终效果 | 已准备下方演示视频脚本，可按步骤录制 |
| 提交制作思考 | 已在第六部分说明问题、取舍与迭代过程 |

## 八、演示视频脚本

建议录制一段 2～3 分钟的视频，按下面顺序展示：

1. 展示 12E4 键盘整体和 8 个 Agent 标签；
2. 键盘接通，本机客户端自动识别，Agent 键显示白色待机；
3. 启动一个飞书 Agent 任务，对应按键变蓝；
4. 任务完成后按键变绿，按下该键打开会话并让灯恢复白色；
5. 按 Codex、Claude Code 键，展示本地应用切换；
6. 演示 Option、Enter、`/stop`、`/new` 四个功能键；
7. 演示 3 号旋钮调音量和按压播放/暂停；
8. 打开 YouTube 或 B 站，先用 4 号旋钮调整文字大小；
9. 按下 4 号旋钮切换到播放速度模式；
10. 连续旋转，展示视频在 1×、1.25×、1.5×等速度间即时切换，同时页面大小保持不变；
11. 按 1 号旋钮关闭键盘灯，作为视频结尾。

视频链接：`（录制完成后填写）`

## 九、总结

AgentPad 把一块普通的 12E4 客制化小键盘变成了日常可用的 AI 工作台。它既能让我一眼看到多个 Agent 的工作状态，也能通过实体旋钮控制音量、播放暂停、网页文字和视频速度。

相较课程案例，我保留了“键盘陪伴工作”的情绪价值，但把它扩展为“键盘帮助管理人与多个 AI 的协作节奏”。媒体播放功能则进一步证明，同一块开放硬件可以跨越 AI 工具、系统控制和网页内容三个场景。这个项目目前已经不是概念演示，而是一套真实安装在本机、可以长期使用并持续迭代的工具。
